# ===========================
# Tarjim - PDF Arabic OCR & Translator
# ===========================

# PDF handling
PyMuPDF>=1.24.0
Pillow>=10.0.0

# OCR - Surya (high-quality Arabic OCR)
# transformers pinned to 4.x: surya-ocr 0.17.x is incompatible with transformers>=5.0.0
surya-ocr>=0.9.0
transformers>=4.57.6,<5.0.0

# Translation - NLLB-200 (primary, 3-5x better quality than Argos)
# CTranslate2 runtime for fast INT8 inference on GPU/CPU
ctranslate2>=4.0.0
# sentencepiece for NLLB tokenizer
sentencepiece>=0.1.99

# Translation - Argos Translate (fallback / lightweight option)
argostranslate>=1.9.0

# Image processing
opencv-python>=4.8.0
numpy>=1.24.0

# Progress bars
tqdm>=4.65.0

# Web API
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Tashkeel - Arabic diacritization (CATT, 2024 SOTA)
# Trained on Shamela classical Arabic corpus (Tashkeela, 75M words, 97 kitab books)
# Model weights (~300-600 MB) are downloaded on first use; subsequent runs are offline.
catt-tashkeel

# Arabic RTL text shaping and bidi reordering for PDF rendering
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# Testing
pytest>=7.4.0
